# 要求・要件定義書

ここではdoppelcatの要求と要件について記述する。

この文書では具体的な実装方法については記述しない。

## 要求定義

### 背景と課題

AIが作成または更新した少数の文書を確認する際、利用者はいったんエディタを開いたり、変更差分を確認するためだけにGitやPull Requestを利用したりする必要がある。この作業は、文書の内容と更新箇所を素早く確認する用途に対して手間が大きい。

### 対象利用者

AIが作成または更新したローカル文書の内容を確認し、必要に応じて軽微な修正を行う利用者を対象とする。

### 目的

doppelcatは、単一のローカル文書について、次の作業をターミナル内で完結できるようにする。

- 文書を継続して閲覧する。
- 文書の更新を自動的に検知する。
- 直前に表示した内容からの変更を見やすく確認する。
- 文書へ軽微な修正を加える。

これにより、文書の確認と軽微な編集のために、エディタ、Git、Pull Requestまたはネットワークサービスを利用する必要をなくす。

### 初期版の対象範囲

初期版は、利用者が明示的に指定した1つのUTF-8テキストファイルの閲覧、更新監視、差分表示および軽微な編集を対象とする。

## 要件定義

### 機能要件

#### FR-001 ファイルの指定と起動

- 利用者は、起動時に既存のファイルを1つ指定できること。
- 起動直後は、指定したファイルの現在内容を通常表示すること。
- 起動直後の内容は差分として扱わないこと。
- 利用者が終了操作を行うまで、表示と監視を継続すること。

#### FR-002 文書の表示

- 文書全体をターミナル上で閲覧できること。
- 文書の更新時は、出力を追記するのではなく、同じ画面領域を再描画すること。
- 文書を上下にスクロールできること。
- 文書の先頭および末尾へ移動できること。
- 更新時は、更新前と同じ先頭行番号を可能な限り維持すること。更新後の範囲外となる場合は、文書末尾に収まる位置へ補正すること。

#### FR-003 更新の検知

- 外部プロセスによる対象ファイルの更新を自動的に検知すること。
- doppelcat内で保存された更新も、外部更新と同様に更新として扱うこと。
- 短時間に連続する更新はまとめ、書き込み途中の各状態を順番に表示しないこと。
- 最後に表示した安定状態と、最新の安定状態を比較すること。

#### FR-004 差分の表示

- 差分は、更新前と更新後を左右に分割せず、1列の行単位差分として表示すること。
- 追加行と削除行を異なる色で表示すること。
- 追加行には `+`、削除行には `-` の記号を表示し、色を識別できない場合でも変更種別を判別できること。
- 削除行は、更新後の文書内で削除された位置が分かるように、更新前の内容を差分表示内に示すこと。
- 差分表示と最新内容の通常表示を、キー操作によって切り替えられること。
- 新たな更新を検知した場合は、直前の更新後状態と最新状態を比較すること。
- 差分表示中は文書を編集できないこと。

#### FR-005 閲覧モードと編集モード

- 起動時は閲覧モードであること。
- 利用者の明示的な操作によって編集モードへ移行できること。
- 差分表示中に編集を開始する場合は、最新内容の通常表示へ切り替えてから編集モードへ移行すること。

#### FR-006 軽微な編集

- 編集モードでは、次の操作を行えること。
  - カーソル移動
  - 文字の挿入および削除
  - 範囲選択
  - コピーおよび貼り付け
  - 元に戻す、およびやり直す
  - 保存
- 編集内容は、利用者による明示的な保存操作で対象ファイルへ反映すること。
- 未保存の編集がある場合は、その状態を画面上で識別できること。
- 保存された編集は更新として扱い、保存前後の差分を表示すること。

#### FR-007 未保存内容の保護

- 未保存の編集がある状態で終了しようとした場合は、利用者に確認すること。
- 編集中に外部更新を検知した場合は競合を通知すること。
- 競合時は、利用者の明示的な判断なしに、未保存内容または外部更新内容を破棄もしくは上書きしないこと。
- 競合内容を自動的にマージしないこと。

#### FR-008 ファイルが利用できない場合

- 監視中のファイルが削除された場合、または一時的に利用できなくなった場合も、doppelcatを自動終了しないこと。
- ファイルを利用できない間は文書領域を空にし、その状態を利用者に通知すること。
- 同じパスのファイルが再び利用可能になった場合は、監視と表示を再開すること。

#### FR-009 文字コードとファイル形式

- UTF-8テキストファイルを扱えること。
- 保存時は、元ファイルの改行形式、UTF-8 BOMの有無および末尾改行の有無を維持すること。
- UTF-8として正しく解釈できないファイルまたはバイナリファイルを指定された場合は、編集を開始せず、対象外であることを表示して安全に終了すること。

### 非機能要件

#### NFR-001 ローカル完結性

- 通常利用にネットワーク接続を必要としないこと。
- 文書内容を外部へ送信しないこと。
- 外部APIを使用しないこと。
- テレメトリを送信しないこと。

#### NFR-002 対応環境

- Windows、macOSおよびLinuxの一般的なターミナル環境で利用できること。
- 対応環境間で、本書に定義した主要機能を同等に利用できること。

#### NFR-003 更新表示性能

- 10 MB以下の対象文書では、ファイルへの書き込みが落ち着いてから1秒以内に更新内容を表示すること。
- 10 MBを超える文書もファイルサイズだけを理由に拒否しないこと。ただし、更新表示性能は保証対象外とする。

#### NFR-004 データ保全

- 利用者による保存操作以外では、対象ファイルの内容を変更しないこと。
- 読み取り、更新検知または保存に失敗した場合に、対象ファイルや未保存内容を暗黙に破棄または破損しないこと。

#### NFR-005 識別性

- 差分の追加と削除は、色だけに依存せず識別できること。
- 未保存状態、競合状態およびファイルを利用できない状態を、画面上で区別できること。

### 初期版の対象外

次の機能は初期版の対象外とする。

- 複数ファイルの同時利用およびディレクトリ内の自動探索
- Markdown固有の整形表示
- 文字列検索
- ページ単位の移動
- 色、差分表示またはキー操作の設定機能
- 文字単位の差分表示
- AIによる文書生成
- Git操作およびPull Request作成
- 複数人による共同編集
- 画像、PDFおよびOffice文書の表示・編集
- プラグインなどの拡張機構
- 文書の履歴管理
- 競合内容の自動マージ

## 受入条件

### AC-001 基本閲覧

既存のUTF-8文書を1つ指定して起動すると、文書の現在内容が通常表示され、上下スクロールならびに先頭・末尾への移動ができること。

### AC-002 外部更新と差分確認

10 MB以下の対象文書を外部プロセスが更新すると、書き込みが落ち着いてから1秒以内に更新が反映され、直前の安定状態との差分を1列の行単位表示で確認できること。また、キー操作によって差分表示と最新内容の通常表示を切り替えられること。

### AC-003 編集と保存

閲覧モードから編集モードへ移行して軽微な修正を行い、明示的に保存できること。保存後は、その変更が更新として検知され、保存前後の差分を確認できること。

### AC-004 内容の保護

未保存内容がある状態で終了または外部更新が発生した場合、利用者への通知または確認なしに内容が破棄・上書きされないこと。

### AC-005 ファイルの再検出

監視中のファイルを削除すると、doppelcatは終了せず利用不能状態を表示し、同じパスにファイルを再作成すると表示と監視を再開すること。

### AC-006 ローカル完結

基本閲覧、更新検知、差分確認、編集および保存の一連の操作が、エディタ、Git、Pull Request、外部APIまたはネットワークサービスを利用せず、ローカルターミナル内で完了すること。

